const actionKey = '(Project Creation) -';

export const GET_INITIAL_CHECKPOINTS = `${actionKey} get initial checkpoints`;
export const RECEIVE_INITIAL_CHECKPOINTS = `${actionKey} receive initial checkpoints`;
export const UPDATE_CHECKPOINT = `${actionKey} update checkpoint`;
export const ADD_CHECKPOINT = `${actionKey} add checkpoint`;
export const DELETE_CHECKPOINT = `${actionKey} delete checkpoint`;

export const RESET_PROJECT_CREATION = `${actionKey} reset project creation`;
export const SET_PROJECT_NAME = `${actionKey} set project name`;

export const CREATE_PROJECT = `${actionKey} create project`;
export const CLEAR_CREATE_PROJECT_DATA = `${actionKey} create project data`;

export const ADD_PROJECT_USER = `${actionKey} add project user`;
export const DELETE_PROJECT_USER = `${actionKey} delete project user`;
export const UPDATE_PROJECT_USER = `${actionKey} update project user`;