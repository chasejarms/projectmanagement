const actionKey = `(Existing Case) -`;

export const UPDATE_CONTROL_VALUE_EXISTING_CASE = `${actionKey} update control value`;
export const CLEAR_EXISTING_CASE_STATE = `${actionKey} clear existing case state`;
export const SET_CONTROL_VALUE_STATE_EXISTING_CASE = `${actionKey} set control value existing case`;